# Calculator

Calculator is a simple C program that makes computation(addition, multiplication, division, substraction) between two number read from input.

## Installation
Use the command line to compile the project

```bash
make re
```

## Usage

```bash
./calculator
# Enter a simple calcul involving two number. Ex: 10 + 99
~~> 
```
